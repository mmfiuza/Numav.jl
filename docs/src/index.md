# Numav.jl

Numav is a Julia library to perform acoustics and vibrations simulations. Currently, the supported components are:

```@contents
Pages = ["fem-helmholtz.md"]
Depth = 1
```

---

## Installation

[Install Julia](https://julialang.org/install/) then install Numav by typing into the Julia REPL:
```julia
]add Numav
```
